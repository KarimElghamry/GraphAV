import styled from 'styled-components';
import assets from '../../assets';

const Star = styled(assets.images.starIcon)`
  height: 28px;
  width: 28px;
  margin: 0 10px;
  user-select: none;
  fill: white;
  cursor: pointer;
`;

export default Star;
