import styled from 'styled-components';
import assets from '../../../assets';

export const AddIcon = styled(assets.images.addIcon)`
  height: 20px;
  width: 20px;
`;

export const UndirectedIcon = styled(assets.images.undirectedIcon)`
  height: 20px;
  width: 20px;
`;

export const DirectedIcon = styled(assets.images.directedIcon)`
  height: 28px;
  width: 28px;
  fill: white;
  transform: rotate(45deg);
`;
