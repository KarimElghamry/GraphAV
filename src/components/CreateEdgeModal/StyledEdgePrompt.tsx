import styled from 'styled-components';

const StyledEdgePrompt = styled.div`
    align-items: center;
    justify-items: space-between;
    padding-left: 40px;
    padding-right: 60px;
    width: 380px;

`;

export default StyledEdgePrompt;